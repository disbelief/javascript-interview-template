{
  "name": "interview-template",
  "version": "1.0.0",
  "description": "Template for Front-End interviews",
  "main": "app.js",
  "scripts": {
    "build": "browserify src/app.js -t babelify --outfile dist/app.js",
    "build-es5": "browserify src/app.es5.js --outfile dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Lawlor",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "babelify": "^7.2.0"
  }
}
